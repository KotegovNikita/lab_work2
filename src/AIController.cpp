#include "AIController.hpp"
#include <iostream>

/**
 * @brief Makes a move on behalf of the AI player.
 * 
 * This method should contain the logic for the AI to analyze the game state,
 * select the best available action (e.g., play a card, attack, use ability),
 * and execute it through the GameManager interface.
 * 
 * @param game Reference to the GameManager handling the game flow.
 */
void AIController::makeMove(GameManager& game) {
    // TODO: Implement actual AI logic for selecting cards and actions.
    std::cout << "AI making a move...\n";
    // Example placeholder: game.runTurn() or similar AI action.
}

